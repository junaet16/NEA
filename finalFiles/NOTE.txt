*Get AI to come up with good default numbers
*Add comments to what the default numbers actually mean
*Currently, StationLineRelationships has no use, so at the end, show which lines are available at the beginning and end of the journey